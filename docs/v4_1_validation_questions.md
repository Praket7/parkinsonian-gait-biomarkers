# v4.1 frozen validation questions

1. Does the existing score retain a positive standardized severity association when only the healthy reference is resampled?
2. Does it improve participant-grouped held-out severity prediction beyond gait speed?
3. Is the association directionally coherent after each single frozen input is removed?
4. Can the unchanged v4.0.6 model be applied to repeated WearGait sessions with all eight inputs equivalent?
5. In Mobilise-D, are within-person gait speed and stride-length effects more stable than cadence and stride-duration effects under participant-half resampling?

No new feature family, tuning, outcome-trained composite, or external crosswalk is permitted.
